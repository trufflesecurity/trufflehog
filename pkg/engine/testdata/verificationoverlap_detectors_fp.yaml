# config.yaml
detectors:
  - name: detector1
    keywords:
      - sample
    regex:
      api_key: \b(sample-[a-zA-Z-0-9]{59})\b

  - name: detector2
    keywords:
      - ample
    regex:
      api_key: \b(ssample-[a-zA-Z-0-9]{59})\b